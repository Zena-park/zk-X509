# Cargo build
**/target

# Cargo config
.cargo

# Profile-guided optimization
/tmp
pgo-data.profdata

# MacOS nuisances
.DS_Store

# Proofs
**/proof-with-pis.json
**/proof-with-io.json

# Env
.env

# Node
node_modules/
.next/
out/

# Foundry
contracts/cache/
contracts/out/
contracts/broadcast/

# Test certs — private keys and PEM intermediates excluded
certs/**/*.key
certs/**/*.pem
# DER certs and public keys are tracked for reproducible benchmarks/tests
# Regenerate: cd certs && bash generate-test-certs.sh && bash generate-test-crl.sh
